/* @flow */
'use strict';

import React, { Component } from 'react';
import { View } from 'react-native';

const ScriptureView = (props: Object) => {
  return (
    <View style={{flex: 1, backgroundColor: 'yellow'}}>
    </View>
  )
}

export default ScriptureView;
